name: Create release tag

on:
  workflow_dispatch:
  pull_request:
    types:
      - closed
    branches:
      - main
  push:
    branches:
      - main
    paths:
      - "VERSION"
      - ".claude-plugin/plugin.json"
      - ".claude-plugin/marketplace.json"
      - ".codex-plugin/plugin.json"

jobs:
  tag-release:
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'release/v')) || (github.event_name == 'push' && startsWith(github.event.head_commit.message, 'Release v'))
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Create and push tag
        run: |
          version=$(tr -d '\n' < VERSION)
          tag="v$version"

          if git rev-parse "$tag" >/dev/null 2>&1; then
            echo "Tag $tag already exists"
            exit 0
          fi

          git tag -a "$tag" -m "Release $tag"
          git push origin "$tag"
